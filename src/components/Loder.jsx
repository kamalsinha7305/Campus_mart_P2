import { ClipLoader } from "react-spinners";

const Loader = () => (
  <div className="flex justify-center items-center min-h-screen">
    <ClipLoader color="#394ff1" size={50} />
  </div>
);

export default Loader;